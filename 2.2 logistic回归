y^ 表示对结果y的预测，更准确的说y^是一个概率,在输入为x（输入的特征向量）的情况下输出是1(y=1)的概率

y^=P(y=1|x)    

Logistic回归的参数是 w 和 b :

w 和 x 一样都是n_x维的向量

b是一个实数

那么已知 w 和 b 还有输入特征向量 x 如何得到 y^

y^ =w^Tx+b

因为x为特征向量（列向量）所以w需要转置成行向量，这样两者相乘可以得到一个实数。

显然 y^的值并不一定在(0,1)之间,为了保证 0=<y^<=1，引入sigmoid函数

		1
sigmoid(z)= —————————
	     1+e^(-z)

当 z 趋近于正无穷，-z 趋近于负无穷，e^(-z)趋近于 0 ，则sigmoid(z)约等于1
当 z 趋近于负无穷，-z 趋近于正无穷，e^(-z)趋近于无穷,则sigmoid(z)约等于0
所以
y^=sigmoid(w^Tx+b)
则无论 w^Tx + b 的值是多少，都可以保证 y^ 的值在区间[0,1]内
